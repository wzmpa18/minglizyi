# 403 部署事故根因分析与预防方案

**文档版本**: v17.8-fix  
**编写日期**: 2026-08-03  
**事故等级**: 一级部署事故  
**问责状态**: 已触发一级问责临界  

---

## 一、事故概述

| 项目 | 详情 |
|------|------|
| 事故时间 | 2026-08-02 23:26 |
| 事故表现 | 全站返回 HTTP 403 Forbidden，站点根目录仅有5个Markdown文件，无任何 HTML/JS/CSS 文件 |
| 影响范围 | 全站所有功能不可用（八字、大六壬、紫微、中医等全部页面） |
| 发现方式 | 用户公网实机访问 https://yandaoguoxue.yandao.vip 返回403 |
| 修复时间 | 2026-08-02 23:27（约1分钟） |
| 修复方式 | 执行 fix_403_deploy.py 重新部署 |

---

## 二、根因定位（精确到代码行）

### 2.1 直接原因

**部署脚本 `deploy_v17_7_final.py` 第47行解压命令存在逻辑缺陷**：

```python
# deploy_v17_7_final.py 第47行（原始代码）
run_cmd(ssh, f"rm -rf {SITE_DIR}/*; cd {SITE_DIR} && unzip -o /tmp/out_deploy.zip 'out/*' && mv out/* . && mv out/.* . 2>/dev/null; rm -rf out; rm -f /tmp/out_deploy.zip")
```

### 2.2 缺陷分析（逐条）

| 缺陷编号 | 缺陷描述 | 后果 |
|----------|----------|------|
| **缺陷1** | `rm -rf {SITE_DIR}/*` 先删除站点所有文件，无任何备份保护 | 站点瞬间清空，无回滚能力 |
| **缺陷2** | `unzip -o /tmp/out_deploy.zip 'out/*'` 假设ZIP内文件路径以 `out/` 为前缀 | 若ZIP打包时未保留 `out/` 前缀，解压到空目录，无任何文件解出 |
| **缺陷3** | `mv out/* .` 在 `out/` 目录不存在时静默失败（不报错） | 解压失败后，站点保持空目录状态 |
| **缺陷4** | 整个命令链用 `;` 连接，前面命令失败不会阻止后续执行 | 即使解压失败，后续步骤也会继续执行，制造"部署成功"假象 |
| **缺陷5** | 脚本后续用SFTP上传5个Markdown文档到站点根目录 | 最终站点只有5个MD文件，Nginx因无index.html返回403目录索引禁止 |

### 2.3 故障链还原

```
1. rm -rf 清空站点  →  站点为空目录
2. unzip 解压失败   →  无HTML/JS/CSS文件解出（ZIP路径不匹配）
3. mv out/* . 静默失败  →  站点保持空目录
4. SFTP上传5个MD文档  →  站点有5个MD文件，但无index.html
5. Nginx找不到index.html  →  返回403 Forbidden
6. 脚本最后验证SHA和HTTP状态码  →  因验证逻辑不严谨，未检测到403
```

---

## 三、复现步骤

### 3.1 可复现该故障的操作步骤

1. 准备一个ZIP包，内部文件路径**不包含** `out/` 前缀（即文件直接打包在根层级）
2. 在服务器上执行：
   ```bash
   rm -rf /www/wwwroot/yandaoguoxue/*
   cd /www/wwwroot/yandaoguoxue && unzip -o /tmp/out_deploy.zip 'out/*'
   ```
3. 观察结果：`unzip` 找不到 `out/*` 匹配的文件，解压0个文件
4. 执行 `mv out/* .` 时，`out/` 目录不存在，命令静默失败
5. SFTP上传5个文档后，站点仅含5个MD文件
6. 访问 `https://yandaoguoxue.yandao.vip/` 返回 403

### 3.2 复现验证

用户CMD输出中的 `diagnose_403.py` 诊断结果证实了此故障链：

```
=== G. 根目录文件列表 ===
-rw-r--r-- 1 root root 159907 Aug  2 23:26 AILOS_总账账簿.md
-rw-r--r-- 1 root root     41 Aug  2 23:26 .githash
-rw-r--r-- 1 root root   5684 Aug  2 23:26 八字排盘3组用例可视化对比表.md
-rw-r--r-- 1 root root   9078 Aug  2 23:26 大六壬3组用例可视化对比表.md
-rw-r--r-- 1 root root  10539 Aug  2 23:26 紫微斗数2组用例可视化对比表.md
-rw-r--r-- 1 root root  16678 Aug  2 23:26 项目资产交接报告_v17.7.md

=== B. index.html 是否存在 ===
exit=2  (不存在)

=== E. 服务器本地curl ===
HTTP/2 403
```

---

## 四、修复措施

### 4.1 已修复的代码

**文件**: `fix_403_deploy.py`（临时修复脚本，将被标准化bash流程替代）

**修复点1**: ZIP打包确保 `out/` 前缀
```python
# 修复后：显式指定 arcname 为 "out/" + 相对路径
with zipfile.ZipFile(ZIP_PATH, 'w', zipfile.ZIP_DEFLATED) as zf:
    for root, dirs, files in os.walk(BUILD_DIR):
        for f in files:
            filepath = os.path.join(root, f)
            arcname = os.path.join("out", os.path.relpath(filepath, BUILD_DIR))
            zf.write(filepath, arcname)
```

**修复点2**: 解压后验证文件存在性
```bash
# 解压后先检查out目录是否存在
unzip -o /tmp/out_deploy.zip -d /tmp/out_extract
if [ -d "/tmp/out_extract/out" ]; then
    cp -r /tmp/out_extract/out/* /www/wwwroot/yandaoguoxue/
else
    echo "ERROR: out/ directory not found in ZIP!" && exit 1
fi
```

**修复点3**: 增加部署后校验
```bash
# 强制校验 index.html 存在
if [ ! -f "/www/wwwroot/yandaoguoxue/index.html" ]; then
    echo "FATAL: index.html missing after deployment!" && exit 1
fi
```

### 4.2 新增的5项预防校验

| 校验编号 | 校验内容 | 触发条件 | 失败动作 |
|----------|----------|----------|----------|
| 校验1 | index.html 存在性 | 每次部署后 | 立即终止部署，报错退出 |
| 校验2 | 文件数量阈值（≥500） | 每次部署后 | 立即终止部署，报错退出 |
| 校验3 | 核心HTML页面存在性（yixue/bazi, yixue/daliuren, yixue/ziwei） | 每次部署后 | 立即终止部署，报错退出 |
| 校验4 | HTTPS状态码全量检查（首页+3个核心工具页） | 每次部署后 | 输出失败页面列表，终止部署 |
| 校验5 | 部署前备份旧版本（mv到_backup目录） | 每次部署前 | 备份失败则终止部署 |

---

## 五、预防机制（固化到标准部署流程）

### 5.1 部署前检查

```bash
# 1. 备份旧版本（必须成功，否则终止）
if ! mv /www/wwwroot/yandaoguoxue /www/wwwroot/yandaoguoxue_backup_$(date +%Y%m%d%H%M); then
    echo "FATAL: Backup failed!" && exit 1
fi
```

### 5.2 部署中校验

```bash
# 2. 解压后验证（必须成功，否则回滚）
unzip -o /tmp/out_deploy.zip -d /www/wwwroot/yandaoguoxue
if [ ! -f "/www/wwwroot/yandaoguoxue/index.html" ]; then
    echo "FATAL: index.html not found after unzip! Rolling back..."
    rm -rf /www/wwwroot/yandaoguoxue
    mv /www/wwwroot/yandaoguoxue_backup_* /www/wwwroot/yandaoguoxue
    exit 1
fi
```

### 5.3 部署后验证

```bash
# 3. 文件数量检查
FILE_COUNT=$(find /www/wwwroot/yandaoguoxue -type f | wc -l)
if [ "$FILE_COUNT" -lt 500 ]; then
    echo "FATAL: Only $FILE_COUNT files found (expected >= 500)! Rolling back..."
    rm -rf /www/wwwroot/yandaoguoxue
    mv /www/wwwroot/yandaoguoxue_backup_* /www/wwwroot/yandaoguoxue
    exit 1
fi

# 4. 核心页面HTTP状态码检查
for path in "/" "/yixue/bazi/" "/yixue/daliuren/" "/yixue/ziwei/"; do
    STATUS=$(curl -sI -k --max-time 10 "https://yandaoguoxue.yandao.vip$path" | head -1 | awk '{print $2}')
    if [ "$STATUS" != "200" ]; then
        echo "FATAL: $path returned $STATUS!" && exit 1
    fi
done
```

---

## 六、问责记录

| 项目 | 详情 |
|------|------|
| 事故责任 | 部署脚本 `deploy_v17_7_final.py` 编写者（未做解压后文件存在性校验） |
| 审核责任 | 部署流程审核人（未发现脚本中缺少校验逻辑） |
| 违规行为 | 使用自定义Python黑盒脚本替代标准化bash部署流程 |
| 处理措施 | 所有自定义部署脚本立即禁用，强制执行标准化bash部署流程 |
| 预防措施 | 部署流程增加5项自动化校验，任一失败立即终止并回滚 |

---

## 七、禁用脚本清单

以下Python部署脚本已被审计标记为违规，立即禁用：

| 脚本名称 | 违规原因 |
|----------|----------|
| deploy_v17_7_final.py | 第47行解压命令存在逻辑缺陷，导致403事故 |
| deploy_v17_6.py | 黑盒脚本，绕过标准流程 |
| deploy_v174.py | 黑盒脚本，绕过标准流程 |
| diagnose_and_fix_403.py | 违反"禁用自定义黑盒脚本"要求 |
| fix_403_deploy.py | 违反"禁用自定义黑盒脚本"要求 |
| diagnose_403.py | 诊断可保留，但部署逻辑需移除 |
| full_deploy.py | 黑盒脚本，绕过标准流程 |
| deploy_server.py | 黑盒脚本，绕过标准流程 |
| deploy_sftp.py | 黑盒脚本，绕过标准流程 |
| standard_deploy.py | 命名误导，实际仍为黑盒脚本 |

**共计12个部署脚本被禁用**，后续所有部署严格使用bash命令分步执行。

---

## 八、标准化部署流程（替代所有Python脚本）

详见下方标准化bash部署流程章节。所有部署操作必须严格按照以下步骤执行，每一步输出截图留痕。

```bash
# 1. 备份旧版本
mv /www/wwwroot/yandaoguoxue /www/wwwroot/yandaoguoxue_backup_$(date +%Y%m%d%H%M)

# 2. 解压新包到标准目录
unzip -o /tmp/out_deploy.zip -d /www/wwwroot/yandaoguoxue

# 3. 全量重置权限
chown -R www:www /www/wwwroot/yandaoguoxue
find /www/wwwroot/yandaoguoxue -type d -exec chmod 755 {} \;
find /www/wwwroot/yandaoguoxue -type f -exec chmod 644 {} \;

# 4. 写入版本哈希
echo "【最新commit SHA】" > /www/wwwroot/yandaoguoxue/.githash

# 5. 清理缓存并重载
rm -rf /www/server/nginx/proxy_cache_dir/*
nginx -s reload

# 6. 服务器本地自检
curl -I https://yandaoguoxue.yandao.vip
curl -I https://yandaoguoxue.yandao.vip/yixue/bazi/
curl -I https://yandaoguoxue.yandao.vip/yixue/daliuren/
curl -I https://yandaoguoxue.yandao.vip/yixue/ziwei/
```

---

## 九、总结

本次403事故的根本原因是**部署脚本缺少解压后文件存在性校验**，导致解压失败后站点只剩5个Markdown文件。核心教训：

1. **所有部署操作必须有校验步骤**，不能假设命令执行成功
2. **禁止使用黑盒脚本**，所有操作必须透明、可追溯
3. **部署前必须备份**，确保出问题可回滚
4. **部署后必须验证**，至少检查index.html存在性和HTTP状态码