# v25.0.77 iOS 上架打包批次报告（IOS-APPSTORE-FINAL-RELEASE-SEAL-11）

> 批次日期：2026-09-01 ｜ 状态：**代码与 CI 链路全部收口，唯一阻塞 = 云签名权限（需 Admin 密钥）**

## 一、批次目标

按 App Store 规则完成 iOS 打包全链路：合规改造 → 云构建 → 分发签名 → TestFlight 上传。

## 二、完成状态总览

| # | 事项 | 状态 | 验证方式 |
|---|------|------|----------|
| 1 | 旧指令（微信服务号 SEAL-10）核实 | ✅ 13/15 步建成；2 步需服务号认证 | 部署报告 + 公网验证 |
| 2 | iOS 支付合规（App Store 3.1.1） | ✅ 前后端三层关闭 | 代码核验 + 公网 403 验证 |
| 3 | Info.plist 权限描述补全 | ✅ 定位权限 + arm64 | 本文件 §三 |
| 4 | 隐私清单 PrivacyInfo.xcprivacy | ✅ 6 类数据类型 | 本文件 §三 |
| 5 | 审核风险入口治理（2.5.2 / 4.2） | ✅ 6 页面 iOS 壳内隐藏 | 构建产物 grep 校验 |
| 6 | Web v25.0.77 构建部署切流 | ✅ 公网版本验证 + records fixture 9/9 | 公网验证 |
| 7 | iOS CI 构建链（GitHub Actions） | ✅ 未签名归档 + 降级产物 | run 33517655248 |
| 8 | 分发签名（exportArchive） | ⛔ Cloud signing permission error | run 33517655248 日志 |
| 9 | TestFlight 上传 | ⏸ 依赖 #8 | — |

## 三、模块交付验证说明

### 3.1 iOS 支付合规（前端开关）
- 文件：`src/lib/platformGate.ts`
```typescript
export const IOS_PAYMENT_ENABLED = false;
export const IOS_PAYMENT_DISABLED_TIP = "iOS 版暂未开放购买功能，您已获得的会员权益登录后可正常使用。";
```
平台矩阵：`payment.ios = false`、`store.ios = false`（会员页店铺入口 iOS 壳内不展示）。

### 3.2 iOS 支付合规（服务端强制拦截）
- 文件：`backend_deploy/platformFeatureGate.js`（已部署，公网验证返回 403）
```javascript
payment: { web: true, android: true, ios: false, wechat: true, qq: false, unknown: true },
```

### 3.3 Info.plist 权限与设备架构
- 文件：`ios/App/App/Info.plist`
```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>用于罗盘测量时获取当前位置，计算真北方位的磁偏角修正。不使用罗盘时不会访问定位。</string>
<key>UIRequiredDeviceCapabilities</key>
<array><string>arm64</string></array>
```

### 3.4 隐私清单（与 ASC 素材包一致）
- 文件：`ios/App/App/PrivacyInfo.xcprivacy`
- 声明的收集数据类型（均 AppFunctionality 用途、Linked=true、Tracking=false）：
  - 手机号、邮箱地址、出生日期、精确位置、其他用户内容、照片或视频

### 3.5 iOS 壳内入口治理
- 新增：`src/lib/iosNativeGate.ts`（useIOSNativeShell hook，useEffect 后置判定避免水合不一致）
- 改造页面：`login`、`register`（隐藏微信登录）；`profile`（隐藏微信绑定/安卓引导）；`download`、`friend`（整页替换，不出现安卓 APK 下载）；`promote`（文案合规）

### 3.6 CI 工作流（归档/签名分离）
- 文件：`.github/workflows/ios-build.yml`
- 签名策略：`xcodebuild archive CODE_SIGNING_ALLOWED=NO`（未签名归档）→ `xcodebuild -exportArchive -allowProvisioningUpdates`（经 ASC API 自动创建 Apple Distribution 证书 + App Store profile）
- KEY_ID 支持仓库 secret `ASC_KEY_ID` 覆盖（commit 5a81405）：更换 Admin 密钥无需改代码
- 构建环境：macos-15-arm64 + Xcode 16.4（Capacitor 8.5.0 二进制框架要求）+ Node 22 + npm ci 缓存

### 3.7 版本号封板
- Web：`package.json` = v25.0.77
- iOS：`MARKETING_VERSION = 1.0`（首版）、`CURRENT_PROJECT_VERSION = 1`
- Bundle ID：`com.yandao.guoxue` ｜ Team ID：`WM586465ZD`

## 四、CI 构建链最终状态（run 33517655248，commit 1d541849）

已打通环节：Checkout → Node 22 → Xcode 16.4 → npm ci → next build（静态导出 + 原生 API 改写脚本内联校验）→ cap sync（内置资源校验 + 无 server.url 残留校验）→ **未签名归档成功** → 降级产物上传成功（未签名 xcarchive，12.5MB，Artifact ID 9804369244）。

**失败环节：Export ipa（exit 70）**
```
error: exportArchive Cloud signing permission error
error: exportArchive No profiles for 'com.yandao.guoxue' were found
error: exportArchive No signing certificate "iOS Distribution" found
```

**根因**：现用 ASC API 密钥（Key ID `UWQ354QP54`）角色为 **App Manager**，无云签名权限——无法经 API 自动创建分发证书与 profile。Apple 规定密钥角色创建后不可修改，必须新建 **Admin** 角色密钥。

## 五、OWNER_ACTION（唯一阻塞，其余全部就绪）

1. **新建 Admin 角色的 App Store Connect API 密钥**（App Store Connect → 用户和访问 → 集成 → 密钥 → 「+」，角色选 Admin，下载 .p8 并记录 Key ID；Issuer ID 与现密钥相同）
2. **确认 App Store Connect 已创建 App 记录**（我的 App → 新建：名称「言道国学」、简体中文、Bundle ID `com.yandao.guoxue`、SKU 自定）——TestFlight 上传前置条件
3. 将新密钥 Key ID 与 .p8 全文提供后，由助手配置 GitHub Secrets（`ASC_KEY_ID` / `APP_STORE_CONNECT_KEY`）并触发 `upload_testflight=true` 的构建

## 六、提审就绪清单（TestFlight 通过后）

- ASC 上架素材包：`docs/materials/20260820_ASC上架素材包_v25.0.44.md`（隐私数据类型表格已与 PrivacyInfo.xcprivacy 对齐）
- 审核备注要点：排盘记录规则（非会员本地/会员云端）、iOS 支付未开放说明、真太阳时/罗盘定位用途说明