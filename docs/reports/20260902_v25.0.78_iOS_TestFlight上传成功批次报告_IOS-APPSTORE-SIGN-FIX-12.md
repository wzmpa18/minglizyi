# v25.0.78 iOS TestFlight 上传成功批次报告（IOS-APPSTORE-SIGN-FIX-12 → TESTFLIGHT-VALID）

> 批次日期：2026-09-02 ｜ 状态：**✅ TestFlight build 1.0(1) 上传成功，Apple 处理 VALID**

## 一、批次成果

**iOS 打包全链路打通**：代码 → GitHub Actions 云构建（Xcode 26.3）→ 手动签名 → TestFlight 上传 → Apple 处理完成（VALID）。
- App：言道国学（ASC id 6807592575）
- 版本：iOS 1.0 (build 1)，最低系统 iOS 15.0
- 成功 run：33576919156（commit b24fa1d）
- 签名 IPA artifact：GitHub Actions → yandao-guoxue-ios-ipa

## 二、根因诊断（API 实测，推翻前判）

2026-09-01 所记「ASC 密钥为 App Manager 角色无云签名权限」为误判。2026-09-02 用密钥直连 ASC API 实测：

| 探测项 | 结果 |
|---|---|
| ES256 JWT 认证 | ✅ 有效 |
| GET /v1/users（Admin-only） | ✅ 200（账号持有人 ZHIMIN WU，ACCOUNT_HOLDER+ADMIN） |
| GET /v1/apps | 「言道国学」bundleId=**com.yandaoguoxue.app** |
| GET /v1/bundleIds | 团队仅注册 com.yandaoguoxue.app + 通配符；**com.yandao.guoxue 从未注册** |
| POST /v1/certificates (DISTRIBUTION) | ✅ 201（Admin 级写权限实锤） |
| POST /v1/profiles (IOS_APP_STORE) | ✅ 201 |

**真因链**：工程配置的 bundle ID `com.yandao.guoxue` 未在开发者账号注册 → exportArchive 云签名自动注册链路报 `Cloud signing permission error`；叠加 ASC 2026 新政「必须 iOS 26 SDK（Xcode 26+）构建才可上传」，Xcode 16.4 产物被 altool 409 拒绝。

## 三、修复链（4 次迭代，全部验证）

### 修复 1：Bundle ID 对齐（commit 4310300）
- `ios/App/App.xcodeproj/project.pbxproj`：PRODUCT_BUNDLE_IDENTIFIER 两处 → `com.yandaoguoxue.app`
- `codemagic.yaml`：bundle_identifier 同步对齐
- Android 包名 `com.yandao.guoxue` **保持不变**（已发布 APK 不受影响）

### 修复 2：签名方案资产化（commit 4310300）
弃用云签名，改手动签名（确定性、可复现、不依赖 xcodebuild 云签名行为）：
- 经 ASC API 创建 Apple Distribution 证书（id 6YJFBTVSBX，私钥本地持有，有效期至 2027-09-02）
- 经 ASC API 创建 App Store profile（name `yandao guoxue iOS App Store 20260902`，uuid 917802a5-6e9b-432d-9710-ca862a4007fc，有效期至 2027-09-02）
- 签名资产入 GitHub Secrets：IOS_DIST_CERT_P12_B64 / IOS_DIST_P12_PASSWORD / IOS_APPSTORE_PROFILE_B64
- CI 流程：未签名归档（CODE_SIGNING_ALLOWED=NO）→ keychain 导入 .p12 + 安装 profile → exportArchive manual（provisioningProfiles 指定）

### 修复 3：macOS 兼容性两连坑（commit 259f6d8）
- OpenSSL 3 默认 P12 HMAC-SHA256 MAC，macOS security 报 `MAC verification failed` → 用 `-macalg sha1 -certpbe PBE-SHA1-3DES -keypbe PBE-SHA1-3DES` 重新导出 .p12
- PlistBuddy 读管道 /dev/stdin 不可 seek（`Cannot parse a NULL or zero-length data`）→ 改 `plutil -extract UUID raw` + UUID 常量一致性校验

### 修复 4：altool 参数 + Xcode SDK（commits 99040ad / b24fa1d）
- Xcode 16 altool 参数为驼峰：`--api-issuer` → `--apiIssuer`
- ASC 2026 强制 iOS 26 SDK → Xcode 16.4 → **26.3**（macos-15-arm64 镜像自带），Capacitor 8.5.0 兼容

## 四、模块交付验证说明

### 4.1 签名资产（本地留存）
路径：`C:\Users\ZhuanZ\AppData\Roaming\TRAE SOLO CN\ModularData\ai-agent\work-mode-projects\6a96bc072584138cb1a16091\ios-signing\`
- `dist.key` + `dist.crt.pem`：分发证书私钥/证书（**核心机密，等同 .p12，勿外传；私钥丢失则证书 2027-09-02 前需重建**）
- `dist.p12`（sha1-MAC 兼容版，已入 Secrets）+ `p12password.txt`
- `917802a5-…mobileprovision`（已入 Secrets）

### 4.2 工作流（.github/workflows/ios-build.yml，关键段）
```yaml
- name: Select Xcode 26.3
  run: sudo xcode-select -s /Applications/Xcode_26.3.app
- name: Import signing assets      # .p12 → keychain，profile → ~/Library/MobileDevice/Provisioning Profiles/
- name: Archive (unsigned)         # xcodebuild archive CODE_SIGNING_ALLOWED=NO
- name: Export ipa (manual signing)  # method=app-store-connect, signingStyle=manual,
  #   provisioningProfiles: { com.yandaoguoxue.app: yandao guoxue iOS App Store 20260902 }
- name: Upload to TestFlight       # xcrun altool --upload-app --apiKey --apiIssuer
```

### 4.3 验证证据
- run 33576919156 全步骤 success（Select Xcode 26.3 → … → Export ipa → Upload ipa artifact → Upload to TestFlight）
- ASC API `GET /v1/builds?filter[app]=6807592575`：版本 1（IOS 1.0），processingState=**VALID**，expired=false

## 五、OWNER_ACTION（TestFlight 内测开测）

1. **添加内测员**：App Store Connect → 我的 App → 言道国学 → TestFlight → 内部测试 → App Store Connect 用户（吴志民已在列，可直接装）→ TestFlight App 安装 build 1.0(1)
2. **出口合规**（build 页面若提示 Missing Compliance）：选「使用标准加密（HTTPS 等）且豁免」常规项即可（App 仅 HTTPS 通信，无自研加密）
3. **真机 E2E 抽查**（通过后再提审）：排盘/记录三态（非会员本地-会员云端）/罗盘定位/立极尺/分享/无微信登录入口/无安卓下载引导
4. 提审素材已就绪：`docs/materials/20260820_ASC上架素材包_v25.0.44.md`（隐私表格已与 PrivacyInfo.xcprivacy 对齐）

## 六、遗留说明

- 账号内存在 3 张 2026-09-01 云签名尝试误建的 Apple Development 证书（"Created via API"），私钥已随 runner 销毁，无 profile 关联，不影响任何流程；如需清理可在 ASC 后台 Certificates 页删除
- 后续版本升级：改 `ios/App/App.xcodeproj/project.pbxproj` 的 MARKETING_VERSION / CURRENT_PROJECT_VERSION（TestFlight 要求 build 号递增），推送后手动触发 workflow（upload_testflight=true）