# v25.0.86 安卓APK构建分发与统一下载地址交付报告

- **交付日期**：2026-09-14
- **版本**：v25.0.86（versionCode 2073）
- **GitHub提交**：7e4c90e（版本号）/ 3e68b84（构建分发脚本，含永久规则）
- **状态**：全部完成，公网独立验证通过

---

## 一、问题背景

用户反馈：打开 `www.yandao.vip/app-download/` 返回 **404**，要求所有下载入口（扫码分享下载、官网下载等）统一指向单一下载地址，更新必须同步，且历史二维码不能失效。

## 二、404根因与修复

| 项目 | 说明 |
|---|---|
| 根因 | www.yandao.vip 由腾讯云COS（北京桶 yandao-guoxue-1300262413）+ CDN 分发，COS对象存储对目录路径（`/app-download/`）无目录索引，直接404 |
| 修复 | 向COS上传 `app-download/index.html` 下载目录页（自动2秒跳转 `latest.apk`，含立即下载按钮、版本号展示），并刷新CDN缓存 |
| 验证 | `https://www.yandao.vip/app-download/` → **200 OK**，标题"言道国学 App 下载（Android v25.0.86）" |

## 三、v25.0.86 APK构建详情

| 项目 | 值 |
|---|---|
| versionCode | **2073** |
| versionName | **25.0.86** |
| 内置资源 | v25.0.86 全量（2071文件：七政四余15专题+38页SEO集群+必应SEO修复+FAQ区块） |
| APK大小 | 14,360,106 bytes（约13.7MB） |
| MD5 | `464ff68f9c42c6e3cce0d512098934e5` |
| 构建时间 | 2026-09-14 18:41:33 +08:00 |
| 签名 | yandao-release.keystore（与历史版本一致） |
| 内容门禁 | 七政四余8chunks / 协纪辨方19 / 手机号码解析17 / 车牌号民俗解读17 / AI免责24，旧"吉凶"文案0残留，全部通过 |

## 四、统一下载地址（唯一规范地址）

> **https://www.yandao.vip/app-download/latest.apk**

## 五、全入口验证清单（19项全部200）

### 5.1 当前主入口
| 入口 | 状态 |
|---|---|
| `https://www.yandao.vip/app-download/latest.apk`（统一地址） | 200，MD5一致 |
| `https://www.yandao.vip/app-download/`（目录页，原404） | **200 已修复** |
| `https://www.yandao.vip/app-download/yandao-guoxue-v25.0.86-release.apk`（版本命名） | 200 |
| `https://yandaoguoxue.yandao.vip/app-download/latest.apk`（App子域） | 301→主域→200 |
| `https://www.yandao.vip/latest.apk`（根路径） | 301→主域→200 |

### 5.2 历史二维码入口（永久有效，禁止删除）
| 历史入口 | 状态 |
|---|---|
| `app-download/guoxue-chuancheng.apk`（**已同步更新为v25.0.86**） | 200 |
| `app-download/guoxue-chuancheng-v1.0-release.apk` | 200 |
| `app-download/guoxue-release.apk` | 200 |
| `app-download/guoxue-release-v2.apk` ~ `v5.apk` | 200 |
| `app-download/guoxue/guoxue-chuancheng-v1.0.apk` / `v2.0.apk` / `guoxue.apk` | 200 |
| `app-download/yandao-guoxue-v25.0.79-release.apk` | 200 |
| `https://yandaoguoxue.yandao.vip/friend`（扫码分享页） | 200 |
| `https://www.yandao.vip/download` | 200 |

### 5.3 官网首页
- 原4处 `yandaoguoxue.yandao.vip/app-download/latest.apk` 已全部替换为 **`https://www.yandao.vip/app-download/latest.apk`** 主域直链，残留子域APK链接0处。

### 5.4 应用内升级
- `https://yandaoguoxue.yandao.vip/api/public/app-version` → `latestVersionCode: 2073`，`downloadUrl` 已统一为主域地址。

## 六、历史二维码兼容性永久规则（已固化到构建脚本）

已写入 `scripts/build_android_v25_0_86.sh` 头部"永久固定规则"及 **[10.5] 历史入口回归检查**步骤，要点：

1. **历史二维码永不失效**：禁止删除/改名COS桶内任何历史APK对象；旧二维码扫出旧版后App内自动提示升级。
2. **每次发版三别名同步**：`latest.apk` + `guoxue-chuancheng.apk` + 版本命名APK 必须同内容同步更新，并刷新CDN缓存。
3. **目录页禁止404**：`/app-download/index.html` 必须可访问。
4. **301收敛**：所有域名的下载URL统一收敛到主域单一路径。
5. **回归校验硬门禁**：构建脚本对16个历史/当前入口URL逐一检查，任一非200即构建失败。

## 七、交付物清单

| 文件 | 说明 |
|---|---|
| `scripts/build_android_v25_0_86.sh` | 构建分发脚本（含永久规则+回归校验），已提交3e68b84并推送 |
| `android/app/build.gradle` | versionCode 2073 / versionName 25.0.86，已提交7e4c90e |
| COS `app-download/index.html` | 目录页（修复404） |
| COS `app-download/latest.apk` 等3别名 | v25.0.86 同步更新 |
| 服务器 `/root/cos_put.js`、`/root/cdn_purge.js`、`/root/cos_ops.sh` | COS上传（带Content-Type）/ CDN刷新 / 列表工具 |
| `docs/reports/20260914_v25.0.86_安卓APK构建分发与统一下载地址交付报告_APK-DOWNLOAD.md` | 本报告 |

## 八、公网独立验证记录（本地Windows外网执行）

1. `curl https://www.yandao.vip/app-download/latest.apk` 下载 → 大小 14,360,106 bytes、MD5 `464ff68f9c42c6e3cce0d512098934e5`，与构建产物完全一致。
2. APK内 `assets/public/app-native.json` → `"versionName": "25.0.86", "versionCode": 2073`。
3. Content-Type → `application/vnd.android.package-archive`（安卓浏览器可直接安装）。
4. 19个入口URL全部200（见第五节）。
5. 版本接口 → `latestVersionCode: 2073`。

## 九、Google Play上架说明

- 上传文件：`https://www.yandao.vip/app-download/yandao-guoxue-v25.0.86-release.apk`（或统一地址 latest.apk，内容相同）。
- versionCode 2073 为目前最高值，可直接作为Play版本号。
- 注意：若Play控制台要求AAB格式而非APK，需另行生成 `.aab`（当前交付为签名APK）。

---

**结论**：v25.0.86 APK已构建、分发、CDN刷新完成；目录404已修复；官网首页4处链接已统一主域直链；19个历史/当前入口全部200，历史二维码全部有效；兼容性规则已固化到构建脚本与项目记忆。
